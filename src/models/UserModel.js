class UserModel {
  constructor(id, memberId, userName, password) {
    this.id = id;
    this.memberId = memberId;
    this.userName = userName;
    this.password = password;
  }

  encryptPassword() {}
}
