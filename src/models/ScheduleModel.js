class ScheduleModel {
  constructor(id, day, startTime, endTime, dojo, count, active) {
    this.id = id;
    this.day = day;
    this.startTime = startTime;
    this.endTime = endTime;
    this.dojo = dojo;
    this.count = count;
    this.active = active;
  }
}
